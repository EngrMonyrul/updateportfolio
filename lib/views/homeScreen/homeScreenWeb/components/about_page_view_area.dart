import 'package:flutter/material.dart';

class AboutPageViewArea extends StatelessWidget {
  const AboutPageViewArea({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('About Page'),
    );
  }
}
