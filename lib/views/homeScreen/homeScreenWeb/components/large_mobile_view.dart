import 'package:flutter/material.dart';

class LargeMobileView extends StatelessWidget {
  const LargeMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('Large Mobile View'),
    );
  }
}
